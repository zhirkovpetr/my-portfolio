import React from "react";
import s from './Nav.module.css';

export const Nav = () => {
    return (
        <div className={s.nav}>
            <a href={''}>HOME</a>
            <a href={''}>SKILLS</a>
            <a href={''}>WORKS</a>
            <a href={''}>CONTACTS</a>
        </div>
    );
}


